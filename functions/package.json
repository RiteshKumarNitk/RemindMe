{
  "name": "medireminder-functions",
  "description": "DoseWise Cloud Functions: scheduled medicine reminders (FCM) + caregiver missed-dose alerts.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions,firestore:indexes"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
